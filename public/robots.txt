# Allow all search engines to crawl everything
User-agent: *
Disallow:

# Sitemap location
Sitemap: https://dppiping.com/sitemap.xml

# Optional: crawl delay for polite scraping (seconds)
Crawl-delay: 5
